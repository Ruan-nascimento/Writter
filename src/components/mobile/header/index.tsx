import { HeadNavigator } from "./navigation";
import { HeadNavWrapper } from "./navWrapper";
import { HeadWrapper } from "./wrapper";

export const Header = {
  root : HeadWrapper,
  iconLink: HeadNavigator,
  navLink: HeadNavWrapper,
}